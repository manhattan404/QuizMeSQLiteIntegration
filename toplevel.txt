# Import the Tkinter module
import Tkinter as tk

# Create a function that will be called when the login is successful
def on_login_success():
  # Close the previous window
  root.destroy()

  # Create a new window
  new_window = tk.Tk()
  # Set the title of the new window
  new_window.title("My New Window")
  # Add any other widgets that you want to include in the new window
  # ...

# Create the root window
root = tk.Tk()

# Create a log in page
# ...
# When the login is successful, call the on_login_success function
# ...
